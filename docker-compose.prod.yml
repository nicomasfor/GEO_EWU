services:
  web:
    build:
      args:
        VITE_CONVEX_URL: ${VITE_CONVEX_URL:-}
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-}
    ports:
      - "${FRONTEND_PORT:-5173}:80"

  api:
    ports: []
